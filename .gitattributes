# Git LFS configuration for large data files

# Only the essential optimized index files (production-ready, compressed)
public/address-index.json.gz filter=lfs diff=lfs merge=lfs -text
public/parcel-metadata.json.gz filter=lfs diff=lfs merge=lfs -text
