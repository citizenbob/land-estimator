# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/node_modules/.cache
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
!.yarnrc.yml

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
logs/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Storybook
/.storybook-cache
/storybook-static
.storybook/*

# Cypress
/cypress/videos/
/cypress/screenshots/

# CI/CD
/dist/
/.turbo/

# Editor and OS files
.vscode/
.idea/

*storybook.log*

# Large data files - track compressed versions only
# Generated uncompressed files - exclude (we only need .gz versions)
/public/parcel-metadata.json
/public/address-index.json
/public/address-index.backup.json
/public/parcel-metadata.backup.json

# Keep essential GIS source files for processing
# Note: Shapefiles and CSV files are kept for processing

# Exclude large intermediate processed files (keep essential source JSON files)
src/data/saint_louis_city/saint_louis_city-processed.json
src/data/saint_louis_county/saint_louis_county-processed.json
src/data/saint_louis_county/saint_louis_county-optimized.json
src/data/parcel_geometry_index.json
src/data/*-processed.json
src/data/*-optimized.json
src/data/*.geojson

# Keep essential build source files:
# - src/data/address_index.json (when regenerated)
# - src/data/parcel_metadata.json (when regenerated)